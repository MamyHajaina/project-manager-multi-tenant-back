package com.back.projectmanagermultitenantback.Services;

import org.springframework.stereotype.Service;

@Service
public class PayrollService {
//    @PreAuthorize("hasRole('ADMIN') or (hasRole('MANAGER') and #deptId == authentication.principal.departmentId)")
//    public Payroll getPayroll(Long deptId) { ... }
}